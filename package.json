{
  "name": "pepewitch",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "install": "cd functions && npm install && cd ../frontend && yarn",
    "start": "concurrently --kill-others --names FUNCTIONS,FRONTEND \"cd functions && npm start\" \"cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "firebase-tools": "^7.0.2"
  }
}
